# build output
dist/
# astro generated types + cache
.astro/
# dependencies
node_modules/
# logs / os
*.log
.DS_Store
# env / secrets — never commit these
.env
.env.*
# content-handoff working folders (source material, not part of the site)
portfolio_handoff*/
